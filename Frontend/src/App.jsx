/**
 * App.jsx is no longer the application entry point.
 *
 * The router is now defined in src/router/index.jsx using
 * createBrowserRouter (React Router v7/v8 Data Router API).
 *
 * main.jsx mounts <RouterProvider router={router} /> directly.
 * The root layout shell lives in src/layouts/RootLayout.jsx.
 */
